<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/fond"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView android:text="prediction" android:layout_width="wrap_content"
        android:id="@+id/predict"
        android:layout_height="wrap_content" />

    <Button
        style="@style/bouton"
        android:layout_centerHorizontal="true"
        android:text="test_record"
        android:id="@+id/test_record"
        android:background="@drawable/bouton"
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:layout_below="@id/predict"
        android:layout_marginTop="30dp"
        />

    <Button
        style="@style/bouton"
        android:layout_centerHorizontal="true"
        android:text="test_predict"
        android:id="@+id/test_predict"
        android:background="@drawable/bouton"
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:layout_below="@id/test_record"
        android:layout_marginTop="30dp"
        />


</RelativeLayout>
